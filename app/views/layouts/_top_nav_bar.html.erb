<div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="container">
        <div class="navbar-header">
            <a class="navbar-brand" href="#">Nishant</a>
        </div>
        <div class="navbar-collapse collapse">
            <% if user_signed_in? -%>
                <p class="navbar-text navbar-right">Signed in as <%= link_to "#{current_user.name} #{current_user.surname}", edit_user_registration_path, :class => "navbar-link" %>  | <%= link_to 'Sign out', destroy_user_session_path, :method => :delete %></p>
            <% else %>
              <p class="navbar-text navbar-right"><%=link_to "Sign in", user_session_path, :class => "navbar-link"%> | <%=link_to "Sign up", edit_user_registration_path, :class => "navbar-link"%></p>
            <%end%>
        </div>
    </div>
</div>